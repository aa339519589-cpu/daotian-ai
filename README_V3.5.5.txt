版本名：V3.5.5 Conservative Render + Settings Fix

这版修正上一个包的问题：
1. 不再做图片/视觉稿。
2. 不再把重点放在“看起来像设置页”的截图效果。
3. 继续沿用 app.js 的原有结构，在现有功能基础上补：渲染、设置拆分、参数、记忆管理。

上传文件：
- app.js

不要上传图片。
不要上传 README。
README 只是说明。

这版改动：
- 左下角入口继续拆成：设置 / 模型提供方。
- 设置页保留 Temperature、Top P、上下文策略、上下文长度、系统 Prompt、人格 Prompt、自动记忆、记忆管理。
- 模型提供方保留 Base URL、API Key、模型名、请求路径。
- 修复 Markdown 过碎问题：不会再把单独的 -、<、=、> 弄成一堆散段落。
- 增加 Markdown 表格渲染。
- 增加数学公式区域横向滚动。
- 增加 AI 回复下方复制、喜欢、不喜欢、重新生成按钮。
- 底部输入框继续预留空间，避免压住最后内容。

操作：
下载 ZIP → 解压 → 只拖 app.js 到 GitHub 仓库根目录覆盖 → Commit changes → 等 Render 跑完 → 刷新稻田 Ai 测试。

注意：
如果仓库里 app.js 已经被其它版本大改过，这个包仍然是覆盖包。出现异常时，先回滚到上一个绿色稳定版本，再覆盖这个 app.js。
