V3.6.1 Stream First Clean Provider Rebuild

只上传 app.js。

重点：
1. 流式输出仍然是第一基准：请求体固定 stream:true，SSE 逐段读取。
2. 生成中只更新最后一条 AI 消息 DOM，不再整页反复重渲染，避免流式抖动和动画乱跳。
3. 模型提供方固定为多个供应商 + 每个供应商多个模型。
4. 给当前供应商新增模型时直接追加，不覆盖旧模型。
5. 主页联网搜索旁边保留快速模型切换，使用原生 select，不使用弹窗动画。
6. 修侧边栏展开/收起布局。
