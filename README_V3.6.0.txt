V3.6.0 Stream First Provider Restore

只上传 app.js。

修复重点：
1. 流式输出为第一基准：OpenAI 兼容请求继续 stream:true，使用 ReadableStream + SSE 逐段读取。
2. 恢复多个供应商 + 每个供应商多个模型。
3. 保存供应商时只新增/合并，不会覆盖掉其他供应商，也不会因为新增模型删旧模型。
4. 主页联网搜索旁边恢复为稳定的原生模型下拉框，去掉自定义弹窗动画。
5. 侧边栏展开/收起加了宽度保护，避免展开后布局错位。

不上传 README。
